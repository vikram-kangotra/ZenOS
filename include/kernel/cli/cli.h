#pragma once

/**
 * @brief Start the Command Line Interface
 * 
 * This function starts the CLI and runs in an infinite loop,
 * processing user input and executing commands.
 */
void cli_run(void); 